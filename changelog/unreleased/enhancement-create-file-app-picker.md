Enhancement: Let users pick an app when creating a file several apps can open

Creating a file with an extension that more than one editor registers for
always opened the first one that happened to be registered, with no way to
choose. The create dialog now offers the candidates when there is a choice and
keeps opening directly when only one app can handle the extension.

https://github.com/cernbox/web
